const Button = () => {

    let title = 'title'

    const func = (value: string) =>{
        console.log(value)
    }

  return (
    <button>{title}</button>
  )
}

export default Button